#include "defs.h"
#include "instructions.h"

extern void setupCPUInstructions();

// dbr is the same db
// d is the same dp
// pb is the same pbr
// x and y are of the same type

extern u16 pc;
	//a;
	//x, y;
	//s;
	//dbr / db;
	//d / dp;
	//pb / pbr;
